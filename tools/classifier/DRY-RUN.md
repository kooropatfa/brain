# Classifier dry-run — expected decisions on the sample captures

A record of what the ingestion classifier (`PROMPT.md`, run on `claude-opus-4-8`) is expected to
decide for each of the two worked sample captures, derived by walking `PROMPT.md` §0–§7 against the
deterministic context pack (`context.mjs`). This is the behavioral contract the live Action must
match; it doubles as the acceptance check for the milestone `done_when`.

> Generated by reasoning through the spec, not by invoking the model (the local environment has no
> `ANTHROPIC_API_KEY`; the live run happens in CI via the Action with the repo secret). Re-validate
> against the actual PR the first time the Action fires.

## `_inbox/meet-2026-06-09-1400.md` — per-workspace pricing decision

Context-pack signals: `schema_ok`, `hint: none`, decision language present ("we're moving to
per-workspace pricing… Decision's made on direction"), supersede present ("supersedes the spring
pricing decision"), action-item owners present (Bob, Carol, Dave).

| Spec step | Expected decision |
|-----------|-------------------|
| §1 dimension | **business** (primary) — pricing/packaging/business-model; cross-links to product (packaging change) |
| §2 kind | **ADR** — a direction was committed to |
| §2 secondary | the implied packaging spec becomes a **follow-up issue**, not a second note |
| §3 file | `decisions/<adr-slug>.md` (matching the folder's existing ADR convention), NOT a dimension folder |
| §4 supersede | bidirectional — new ADR `supersedes:` the spring pricing ADR; old ADR flipped to `superseded_by:`. If the prior ADR can't be found, say so in the PR + open an issue to locate it. |
| §6 follow-up issues | **3** — `[follow-up] Bob: revenue model (by Fri)`, `[follow-up] Carol: 3 churned trial accounts`, `[follow-up] Dave: spec per-workspace packaging + migration` |
| §5 move | capture removed from `_inbox/`; `_TEMPLATE.md`/`README.md` untouched |
| §7 PR | `Ingest: Pricing review — per-workspace tiers`; not merged |

**Satisfies `done_when` end to end:** moves out of `_inbox/` ✓ · files a distilled note under the
correct location (decisions/) ✓ · valid `[[cross-links]]` incl. the supersede ✓ · ≥1 follow-up issue
(3) for the action items ✓.

## `_inbox/manual-2026-06-09-1030.md` — "shared workspaces" idea

Context-pack signals: `schema_ok`, `hint: product`, no decision language, exploration language
present ("Idea:", "Open questions I don't have answers to:"), explicit no-action signal ("Not asking
anyone to build this — just want it captured"), author names product + user + design.

| Spec step | Expected decision |
|-----------|-------------------|
| §1 dimension | **product** (primary) — honors the `product` hint because the content agrees; cross-links to user + design |
| §2 kind | **research / note** — an idea with open questions, nothing decided, nothing to build yet |
| §3 file | `product/<slug>.md` (the dimension folder, NOT decisions/) |
| §4 cross-link | `[[user/...]]` + `[[design/...]]`; additive one-line backlink in `product/index.md` MOC |
| §6 follow-up issues | **0** — the capture explicitly asks for no action ("Not asking anyone to build this"). Creating none is correct, not a miss. |
| §5 move | capture removed from `_inbox/` |
| §7 PR | `Ingest: Idea — surface shared workspaces to cut onboarding drop-off`; not merged |

This proves the other half of the contract: a capture with **no** action item produces **no** issue —
the classifier doesn't manufacture follow-ups.

## What this dry-run establishes

- The spec is **unambiguous** on both sample captures — the classifier reasons from the context pack's
  facts (config-driven dimensions, parsed frontmatter, on-disk targets) rather than guessing.
- The two captures jointly exercise every `done_when` clause **and** the negative case (no action item
  → no issue), plus the ADR-vs-note split and the superseding cross-link.
- If a future capture leaves the dimension genuinely under-determined, that's the "fix the structure,
  not the classifier" signal (roles-and-rhythm §5) → open a thread to m2, don't guess in code.
